#include <stdio.h>

// main - A c program to assigning an address to a pointer variable.

int main(void) {

int *p, c; //declaring a pointer *p and a variable c
c = 5;  //assigning value to variable c
p = &c; //address of c is assigned to pointer p

return 0;

}
